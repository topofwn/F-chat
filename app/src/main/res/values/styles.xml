<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:homeAsUpIndicator">@mipmap/ic_arrow_back_nav</item>
    </style>

    <style name="AppDialogTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppThemePopUp" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="android:homeAsUpIndicator">@mipmap/ic_delete_white</item>
    </style>


    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/normal_color</item>
        <item name="colorControlActivated">@color/active_color</item>
    </style>

    <style name="PauseDialogAnimation">
        <!--<item name="android:windowEnterAnimation">@anim/slide_up_dlg</item>-->
        <!--<item name="android:windowExitAnimation">@anim/slide_bottom</item>-->
    </style>

</resources>
